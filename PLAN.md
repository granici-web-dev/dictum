# PLAN — фазы и текущая задача

Правило: сначала сквозной путь на самом простом входе, потом всё остальное. Ворота, диалог, база и
ресёрч добавляются только когда путь целиком доезжает до Trello.

## Где мы
Бот в polling принимает текст, голосовое до 2 минут и аудиофайл до 20 минут (с вопросом о согласии
до скачивания), расшифровывает запись через Whisper и отдаёт разбор встречи со всеми поручениями. Под
каждым поручением кнопка «Разложить на шаги» ведёт дочерний прогон через ворота до одной карточки в
списке `Assignments`; под оглавлением кнопка «Проработать как идею» ведёт старый путь идеи до
карточек на доске `TRELLO_IDEA_BOARD_ID`. Ворота, brief-диалог, выбор идеи и состояние прогонов
живут в Postgres и переживают перезапуск бота.

В работе P3-08: фазы 1 и 2 написаны и проверены вживую на немецком тексте, открыты живая проверка
фазы 1 записью файлом и русским голосовым, живая проверка части E (путь идеи в боте) и фаза 3 про
записи длиннее 20 минут. Следом P3-11: части A–D написаны и проверены вживую, открыты части E и F.

## Очередь

Порядок задан и держится: следующая задача — первая незакрытая строка списка.

1. - [ ] P3-08 Пересмотр под новую цель: проект стал личным инструментом разбора записанных созвонов на немецком (`CLAUDE.md`, `SPEC §1`). Три вопроса закрыты решениями владельца 17.09.2026, работа разбита на три фазы, у каждой свой shape и craft
   - [x] Язык результата, форма результата и внешние сервисы — подробности в docs/plan-log.md
   - Факты для решения. Лимит файла в 20 минут держит не Whisper: перекодировка даёт 24 кбит/с (перемерено 17.09.2026: 60 с → 180 КБ), час весит около 11 МБ при пределе Whisper 25 МБ. Поднять лимит мешает таймаут запроса 120 с в `app/transcribe.py`. Настоящее узкое место — предел Telegram в 20 МБ на скачивание (`getFile`): 20 МБ несжатого wav — это около 10 минут
   - [x] Фаза 1, разбор встречи стадией `review` — подробности в docs/plan-log.md
     - [ ] Живая проверка, закрывает фазу. Каждый вызов только с отдельного «ок» владельца: `make db` (база стоит на `0003_brief_dialog`, голова `0004_consent_confirmed`); немецкий текст `fixtures/transcript_meeting_de.md` без frontmatter сообщением (правило перевода здесь проверяет промпт, не код: у текста `lang` это `DEFAULT_LANG`); немецкая запись встречи 5–10 минут файлом (заодно не хоженный вживую путь P3-02); русское голосовое с одним поручением (перевода нет). По каждому: токены, был ли ремонт, `unverified=<N>` и чем был каждый помеченный фрагмент, время от «Да» до последнего документа. Качество перевода и сути, полноту поручений и честность пометок оценивает владелец
       - [x] Немецкий текст сообщением и доставка разбора ботом проверены 17.09.2026 — подробности в docs/plan-log.md
   - [x] Фаза 2, поручение в карточку и путь идеи, части A–E — подробности в docs/plan-log.md
     - [ ] Живая проверка части E ботом, каждый вызов по отдельному «ок». Сначала «Проработать как идею» без `TRELLO_IDEA_BOARD_ID` в окружении процесса: отказ `idea_board_missing` без единого вызова. Затем с доской идей: русское голосовое со своей идеей, разбор, «Проработать как идею», вопрос брифа, ответ, «Собирай», ворота брифа, «Дальше», ворота задач, «Дальше», карточки фаз на доске идей. По логу: `start=idea`, `stop=answer`, `brief=answer|assemble`, `gate=next`, `finished cards=`, `seconds=`. Закрывает и пункт P2-04 «Живой прогон целиком»
   - [ ] Фаза 3, длинные встречи: запись длиннее 20 минут. Пересобрать P3-09 и P3-03 вокруг встречи как главного входа
2. - [ ] P3-11 Вопросы для тимлида и ресёрч решений в пути поручения. Идёт сразу после живой проверки части E фазы 2 P3-08, раньше фазы 3. Между нажатием «Разложить на шаги» и шагами встают самодостаточные вопросы, которые владелец сам отбирает и отправляет тимлиду, его ответ reply-ем, ресёрч внутри стека рабочего проекта по стандартам Rigorous (или подбор стека нового) и файл для `/rigorous shape` после карточки. Бот тимлиду ничего не пишет. Шесть частей, у каждой живая проверка по отдельному «ок»
   - [x] Части A, B и C, с живой проверкой части B — подробности в docs/plan-log.md
   - [x] Живая проверка части C локально пройдена 17.09.2026, найдено и закрыто два недочёта — подробности в docs/plan-log.md
   - [x] Часть D: вопросы в боте, парковка, reply, текст для копирования, с живой проверкой ботом 18.09.2026 и тремя закрытыми находками — подробности в docs/plan-log.md
     - [ ] Живая перепроверка трёх находок, по отдельному «ок», немецким текстом встречи. Поручение с вопросами, повторное нажатие «Разложить на шаги», затем reply на **первую** копию вопросов: должен продолжиться тот же прогон (`answer=teamlead`), а не завестись новый разбор. Reply на сообщение о ходе прогона или на разбор: отказ `reply_to_bot` без вызова модели. В `review.json` немецкого текста поле `meeting_lang: de`, название в `clarify.json` и шаги на немецком с переводом. Короткое сообщение вроде «Kurze Aufgabe für dich» не даёт в `ask_back` вопроса о том, кто поручил
   - [ ] Часть E: ресёрч `approach` с веб-поиском в двух режимах, кнопка «Шаги без ресёрча»
   - [ ] Часть F: файл `shape_prompt.md` для `/rigorous shape` после «Дальше», собранный кодом
3. - [ ] P3-09 Одна граница длины для голосового и файла. После P3-08 главный вход — запись встречи, и граница решается вокруг неё, вместе с P3-03 (фаза 3 P3-08). Голосовое сегодня ограничено 2 минутами (`MAX_VOICE_MINUTES`), файл — 20 минутами, и у короткого лимита больше нет причины. Граница опирается на факты из P3-08: 20 МБ на скачивание и таймаут 120 с у расшифровки. В коде место помечено `TODO(P3-09)`
4. - [x] P3-10 Живая остановка отказывает новой записи — подробности в docs/plan-log.md
5. - [x] `/gates on|off` меняет умолчание ворот в чате — подробности в docs/plan-log.md
6. - [x] P3-07 Выбор идеи кнопкой вместо сообщения — подробности в docs/plan-log.md
7. - [x] **Серия живых прогонов** 09.09.2026: пять из пяти, три находки закрыты — подробности в docs/plan-log.md
   - [ ] Всё это без живых прогонов: правки сделаны после серии 09.09.2026, проверка — живым немецким прогоном, иначе правило проверено не будет
8. - [x] P2-04 brief-диалог: вопрос, ответ, «Собирай» — подробности в docs/plan-log.md
   - [ ] Живой прогон целиком: голосовое → вопросы → «Собирай» кнопкой → ворота брифа → карточки. Кнопка, остановка в базе и продолжение после перезапуска вживую не ходили. До части E фазы 2 P3-08 пройти его было нельзя: бот не ставил `interactive` и путь идеи из бота не начинался. Закрывается живой проверкой части E
9. - [x] P3-02 Аудиофайл с кнопкой согласия — подробности в docs/plan-log.md
   - [ ] Живая проверка: mp3 как аудио, wav как документ, «Нет» (ни файла в `runs/`, ни строки), файл длиннее 20 минут (`refused`, файла нет), нажатие «Да» в групповом чате не тем, кто прислал (в логе `by=` нажавшего)
10. - [ ] P3-03 Нарезка длинного аудио по тишине, склейка. Пересобирается вместе с P3-09 вокруг встречи как главного входа (фаза 3 P3-08)
11. - [ ] P4-01 `/research` market-режим с веб-поиском; `kind: feature` → skip
12. - [ ] P4-02 Логи токенов и стоимости на run
13. - [ ] P4-03 Деплой на Hetzner, webhook вместо polling
    - [ ] Деплой меняет площадку: задача закрывается живым прогоном целиком, а не по диффу
14. - [ ] P3-05 self-hosted Whisper на Hetzner: сейчас расшифровка уходит в OpenAI, то есть за пределы EU (`SPEC §8`)
    - [ ] Свой Whisper меняет расшифровку: задача закрывается живым прогоном целиком, а не по диффу

## Не в очереди
Ждут решения или повода, порядка между ними нет.

- [ ] Размышление модели у остальных пяти стадий. Гипотеза «лишний выход — это текст мимо тегов» опровергнута замером 17.09.2026: один вызов `steps` с перехватом сырого ответа дал 2946 токенов выхода при 1745 знаках текста, а мимо тегов оказалось 0 знаков до первого тега и 0 после последнего — правило `api_mode.md` модель соблюдает. Лишнее это блок `thinking`: у `claude-sonnet-5` он идёт по умолчанию, без всякого параметра, тарифицируется как выход и растёт с неясностью входа, от нескольких сотен токенов до шести тысяч. Сделано: строка вызова считает `thinking`, `thinking_tokens` и `text_chars`; запись стадии получила поле режима; `clarify` и `steps` идут с `disabled` (`SPEC §7`). Осталось решить по `review`, `intake`, `brief`, `prd` и `decompose`. Их не тронули нарочно: точность дословных цитат разбора проверена пятью живыми прогонами, а путь идеи живой проверки части E ещё не проходил — по каждой решать замером на живом прогоне, а не вслепую
  - [x] Решение по `clarify` и `steps` подтверждено живым сравнением 18.09.2026, прогон `c33db8366f0f0c2d`: тот же учебный тикет `fixtures/ticket_de.md` и те же входы, что 17.09.2026, без стандартов проекта
    - `clarify`: было 25 с и 2417 токенов выхода, стало 6 с и 521 (`thinking=no`, `text_chars=1276`), 3 вопроса: какие поля обязательные, задана ли библиотека, ждут ли автотестов
    - `steps` без ответов: было 73 с и 7240, стало 10 с и 960 (`text_chars=2182`), 5 шагов, пометки вида «[уточнить: Frage 2]» стоят у шагов, зависящих от неотвеченных вопросов
    - `steps` с частичным ответом по-немецки: было 45 с и 4264, стало 13 с и 1174; ответ про библиотеку закрыл вопрос 2, `unanswered` [1, 3]; реплика тимлида про момент показа ошибок не отвечала ни на один вопрос, но не выброшена: она вошла в шаг («erst beim Absenden»)
    - Ремонтных повторов не было, выдуманных шагов нет, «не надо» и критерии приёмки соблюдены. Качество не просело, и владелец подтвердил решение оставить `clarify` и `steps` без размышления. Замер на живом прогоне — тот же способ решать и по остальным пяти
- [ ] Принятый риск: `file_size` в сообщении может не прийти, и тогда файл больше 20 МБ проходит проверку до вопроса, а после «Да» получает «не смог забрать запись» (`FILE_NOT_TAKEN`, строка `failed`) вместо честного `too_big`
- [ ] Принятый риск: согласие даёт любой участник разрешённого чата. Проверки, что нажавший есть на записи, нет: в логе остаётся его `by=`, и только
- [ ] Условные ворота после intake по `confidence: low` (`SPEC §3.2`): признак ворот в списке стадий сегодня безусловный (`gate_after`), а у `idea.md` нет своего краткого содержания
- [ ] Текст сбоя `convert_to_mp3` показывает человеку stderr ffmpeg с полным путём на сервере — та же поломка, что уже починена у `ffprobe` (P3-02)
- [ ] `SPEC §8` обещает «≤ 3 параллельных run на chat_id», а бот держит ровно один прогон за раз с отказом `busy` (`SPEC §7.3`). Одно из двух мест врёт

## Как начинать сессию в Claude Code
Первое сообщение: «Прочитай CLAUDE.md, SPEC.md и PLAN.md. Текущая задача — первая незакрытая строка Очереди. Предложи план на 5-7 шагов, не пиши код до моего ок.»
`docs/plan-log.md` без нужды не открывается: он нужен только тогда, когда надо понять, почему
закрытое решено так, а не иначе.
