# PLAN — фазы и текущая задача

Правило: сначала сквозной путь на самом простом входе, потом всё остальное. Ворота, диалог, база и
ресёрч добавляются только когда путь целиком доезжает до Trello.

## Фаза 1 — Сквозной сценарий (текст → Trello)  ✓ закрыта
- [x] P1-01 Скелет: uv, ruff, mypy strict, pytest, Makefile, docker-compose (postgres, redis), `.env.example` — проверить, что `make test` зелёный на заглушках
- [x] P1-02 `app/stages.py`: чтение промпта из `.claude/commands/`, вызов Anthropic, `StageResult` с файлами стадии (запись на диск — у вызывающего); тесты без сети
- [x] P1-03 `make run-text`: текст → intake → brief(batch, без диалога: «хватит» сразу) → prd → decompose; артефакты в `outputs/`
- [x] P1-04 Валидация `issues.json`: ids уникальны, `depends_on` существуют, нет циклов; тест на фикстуре
- [x] P1-05 `app/trello.py` (транспорт) и `app/publish.py` (отображение): списки по фазам, карточки, чеклист DoD, labels по area, отложенные скоупы в `Backlog`; идемпотентность по маркеру `dictum:<id>` на доске; тесты с замоканным httpx (respx)
- [x] P1-06 Первый реальный прогон на `fixtures/idea_text.md` → доска Trello; скриншот в `docs/`
  - [x] Текст → `issues.json` вживую: 10 issues, 2 фазы, граф без циклов; валидатор чист. Стоило четыре вызова Sonnet, ~55 с на первые три стадии и ~112 с на prd с decompose
  - [x] Публикация в Trello: 16 карточек, повтор без записей, досборка недоделанной карточки — `docs/p1-06-run.md`. Прогон нашёл порядок списков задом наперёд, починено там же
  - [x] Скриншот доски в `docs/trello-board.png`
- [x] P1-07 Сквозные ключи задач `PROJECT_KEY-N`: выдаются при публикации, продолжают нумерацию доски; маркер несёт ключ, `run_id` и локальный идентификатор, поэтому `I-001` разных прогонов больше не сталкиваются. Отложенные скоупы получили `title` от `/decompose` и попадают в `Backlog` с именем, а не с шифром

## Фаза 2 — Демо-путь к 18.09 (ITCS)  ← ТЕКУЩАЯ

**Что показываем на стенде.** Голосовое в Telegram → карточки в Trello. `auto_approve`, один
пользователь, состояние в памяти процесса, статус-сообщение после каждой стадии, ссылка на доску в
конце. Текст — тот же путь, запасной вход, если голосовое не пройдёт при людях.

**Чего на стенде нет.** Файлов с диска и consent, ворот, диалога брифа, Postgres, ресёрча (идёт в
`skip`). Всё это в фазе 3 и не начинается раньше, чем демо отработает.

Задачи идут строго в этом порядке: каждая следующая опирается на предыдущую.

- [x] P2-00 Хребет пайплайна: делать **до** бота, потому что оба пункта дублируются в воркере, как только он появится (`docs/architecture-review.md`)
  - [x] Список стадий данными (`SPEC §7.2`): имя, исполнитель `llm`/`code`, входы, выходы, параметры. `cli` идёт по списку от стадии в `--from`; лесенка `if from_here <= N` уходит, `STAGE_ORDER` и `STAGES` схлопываются в одну запись
  - [x] `research` — запись списка с исполнителем `code`, а не заглушка, записанная в обход списка веткой в `cli`
  - [x] `run_id` от `ingest` (`SPEC §3.1`, `§5`): выдаётся при записи `transcript.md`, доезжает до `issues.json`, `publish` его только читает и падает, если поля нет. Формат маркера не меняется, доску не чистим
  - [x] Ожидание для P2-01: `ingest` и `publish` входят записями в тот же список, а не заводят у бота свой порядок. Пайплайн бота — тот же список от `ingest` до `publish`, отличается только начало и конец обхода
- [x] P2-01 Бот: режется надвое, потому что обход тестируется без Telegram и без сети, а бот — нет
  - [x] Общий обход: `ingest` и `publish` — записи того же списка стадий, `walk` в `app/run.py`, корень артефактов у прогона свой (`runs/<run_id>/` у бота, текущий каталог у CLI). Дорога у бота и у `make run-text` одна, копии порядка больше нет
  - [x] Бот polling: `/start`, текст → весь пайплайн → publish. Статус-сообщение после каждой стадии, ссылка на доску в конце. Состояние прогона живёт в памяти процесса: одного пользователя и одного прогона за раз для стенда достаточно, а Postgres — это P2-02 после ITCS
    - [x] **Первым делом — белый список `chat_id` в `.env`.** Без него бот тратит деньги по сообщению от любого, кто нашёл его по имени, а на конференции это вопрос времени. Условие первого запуска, не отделочная задача
    - [x] Пайплайн синхронный, бот асинхронный (`CONVENTIONS`): обход уходит в `asyncio.to_thread`, иначе бот молчит три минуты
    - [x] Второй прогон при живом первом — отказ с объяснением, а не очередь. Замок оказался недостижим: PTB по умолчанию разбирает обновления по одному, и второе сообщение ждало конца первого прогона молча, а потом заводило свой. Найдено живой проверкой 08.09.2026 (три сообщения подряд → три оплаченных прогона), починено `concurrent_updates(True)` и проверено вживую в тот же день: отказ пришёл сразу, второго прогона не завелось
- [x] P2-05 `auto_approve` режимом прогона: ворот ещё нет, но флаг и его семантика в `SPEC §3.2` уже описаны, и бот идёт именно по этой ветке, а не в обход неё. Когда ворота появятся, включать будет нечего
  - [x] Ворота — поле записи стадии (`gate_after`), а не ветка у вызывающего: колонка «Ворота после» из `SPEC §3` стала данными, и копия порядка не заводится у бота с воркером
  - [x] Обход отдаёт остановку с родом: выбор идеи из нескольких `auto_approve` не снимает — подтверждать там нечего, а выбрать за человека значит выдумать данные. Ограничение демо записано в `SPEC §3.2`
  - [x] `make run-text TEXT="…" ARGS=--gates`: ветку с воротами можно пройти руками до P2-03, а не только в тестах. Рецепт `run-text` получил `ARGS`, иначе флаг было не передать
  - [x] Живая проверка на `fixtures/idea_text.md`: обход встал после brief, код `2`, сообщение назвало артефакт и `--from research`. Прогон нашёл, что логгер CLI звался `__name__`, а под `python -m app.cli` это `__main__` мимо дерева `app`: свои сообщения CLI глотал, и «Идей несколько» человек тоже никогда не видел. Починено там же
- [x] P3-01 Голосовое `.oga` → ffmpeg → Whisper → `transcript.md` → дальше тот же путь, что и у текста. Главный вход демо
  - [x] Расшифровка — стадия `ingest`, а не обработчик бота: `app/transcribe.py` держит границу (ffmpeg и Whisper), `Run.audio` включает ветку, дорога после `transcript.md` у голосового и текста одна
  - [x] Язык прогона называет Whisper: `Run.lang` до расшифровки хранит `DEFAULT_LANG`, после — распознанный, и `brief` получает уже его. Имя языка от Whisper (`russian`) переводится в код таблицей; незнакомое идёт как есть, потому что соврать про язык хуже
  - [x] Бот: голосовое, отказ длиннее 120 с по `duration` из сообщения (до скачивания и до денег), отказ с объяснением на файлы вместо молчания, проверка `ffmpeg` при старте рядом с белым списком
  - [x] Живой прогон 08.09.2026: голосовое 52 с на немецком → 15 карточек на `Dictum Demo`. Whisper 4.7 с, весь путь около трёх минут. Запись за собой убралась, `lang` пришёл от Whisper (`german → de`)
  - [x] Прогон нашёл гонку правок сообщения: последняя правка прогресса приходила после правки со ссылкой и затирала её, а лог оставался чистым. Финальная правка теперь ждёт все правки прогресса; тест на порядок падает на старом коде
  - [x] Голосовое на русском 08.09.2026 (`36c18be3f51087a6`): `lang: ru` при `DEFAULT_LANG=de`, бриф и 14 карточек по-русски — язык доехал от Whisper до доски. Ссылка в чате пришла последней: фикс гонки проверен живым прогоном, а не только тестом
  - [x] Отказ на голосовое длиннее 120 с: ни строки в логе, ни каталога в `runs/`, ни байта в OpenAI — проверка стоит до скачивания. Отказ по длине срабатывает раньше отказа по занятости, потому что он про само сообщение, а не про очередь
  - [x] Прогон нашёл, что ремонтный повтор стадии случается и на живых данных (intake ответил без тегов `<file>`, вторая попытка прошла): в замер времени у стенда его надо закладывать, а не считать три минуты гарантией
- [x] P2-06 Репетиция: пять живых прогонов подряд на `Dictum Demo` с очисткой доски между ними (`make clean-board`, артефакты — `make clean-runs`), замер времени и цены каждого, отчёт в `docs/`. Последняя задача перед стендом: она отвечает на вопрос «сколько человек ждёт у стенда и во сколько это обходится», а не «работает ли»
  - [x] Отчёт — `docs/p2-06-rehearsal.md`. Пять прогонов: дошли четыре, ожидание 213-287 с при медиане 241, цена расчётно $0.19 за прогон. Ответ на вопрос задачи: человек у стенда ждёт около четырёх минут, и больше половины этого времени — decompose
  - [x] Прогон с телефонного хотспота при закрытой крышке прошёл целиком: 241 с, две минуты непрерывного соединения на decompose, ноль сетевых ошибок за всю репетицию. P4-03 остаётся в фазе 4, ноутбук как площадка держит
  - [x] Репетиция нашла три вещи и все три починены в тот же вечер: intake делил одну идею надвое, decompose нарезал бэклог мельче, чем по нему работают, приветствие обещало три минуты вместо четырёх. Промпты проверены тремя прогонами, включая контрольный в обратную сторону

## Фаза 3
- [x] P3-04-lite Бот на кандидатах показывает список найденных идей с номерами и просит прислать нужную отдельным сообщением. Без кнопок: они в P3-07. Ветка встретилась дважды за два дня живых прогонов, а человек не видел, между чем ему выбирать
- [x] P3-06 Контракт `candidates.md`: intake заявляет `outcome: multiple | none`, форма строки объявлена в промпте и проверяется на выходе стадии. Ревью P3-04 нашло, что случай «задания нет» код узнавал по пустоте разбора, а промпт велел писать список и в нём: на записи без задания человек получал темы разговора, поданные как идеи
- [ ] P3-04 Продолжение прогона после выбора: бот держит `chat_id → run_id`, ответ человека (номер или текст) уходит повтором intake с историей и `user_edit` «выбрана идея N», intake пишет `idea.md`, обход продолжается с brief в том же прогоне. Сегодня остановка бросает прогон вместе с расшифровкой, и человек диктует идею заново по памяти. Идёт до P2-02: без него кнопки (P3-07) будут красивым способом начать сначала
  - [x] `Redo` — зеркало `Pause`: остановка выходит из обхода, правка заходит обратно в ту же стадию и только в неё. Прошлый ответ стадии для истории собирается из артефакта, а не хранится сырым: сырой текст прогон не переживёт, а файл переживёт и в P2-02
  - [x] Бот: остановка по `chat_id` в памяти процесса, цифра из списка становится «Выбрана идея N: Название», прочий текст — правкой как есть. Голосовое всегда начинает новый прогон и снимает остановку (на стенде следующий человек говорит голосом), `/start` — тот же сброс, таймаута нет. При `outcome: none` ждать нечего, остановка не заводится
  - [ ] Живой прогон: запись с двумя идеями → выбор номером → карточки в Trello. До него P3-04 не закрыт: продолжение прогона проверяется тем, что бриф собрался по выбранной идее, а не тем, что тесты зелёные
- [ ] P2-02 Модели `runs/artifacts/gates` в Postgres + Alembic; состояние переезжает из памяти процесса в базу. Владение фактами решено заранее в `SPEC §4.1`: строка владеет идентичностью, статусом, языком и решениями ворот, `Run` — представление на время обхода, артефакты остаются файлами
- [ ] P2-03 Ворота с inline-кнопками после brief и decompose; «Править» → `user_edit` → перезапуск стадии
- [ ] P2-04 brief-диалог: вопрос → ответ → следующий вопрос; «Собирай»
- [ ] P3-07 Выбор идеи кнопкой вместо ответа сообщением: поверх продолжения прогона (P3-04) и кнопок ворот (P2-03)
- [ ] P3-02 Файл mp3/m4a: consent-кнопка, отказ без подтверждения
- [ ] P3-03 Нарезка длинного аудио по тишине, склейка
- [ ] P3-05 self-hosted Whisper на Hetzner: сейчас расшифровка уходит в OpenAI, то есть за пределы EU (`SPEC §8`). Для демо это записанное ограничение, для продакшена — нет
- [ ] P4-01 `/research` market-режим с веб-поиском; `kind: feature` → skip
- [ ] P4-02 Логи токенов и стоимости на run
- [ ] P4-03 Деплой на Hetzner, webhook вместо polling

## Как начинать сессию в Claude Code
Первое сообщение: «Прочитай CLAUDE.md, SPEC.md и PLAN.md. Текущая задача — P2-01. Предложи план на 5-7 шагов, не пиши код до моего ок.»
