# PLAN — фазы и текущая задача

Правило: сначала сквозной путь на самом простом входе, потом всё остальное. Ворота, диалог, база и
ресёрч добавляются только когда путь целиком доезжает до Trello.

## Фаза 1 — Сквозной сценарий (текст → Trello)  ✓ закрыта
- [x] P1-01 Скелет: uv, ruff, mypy strict, pytest, Makefile, docker-compose (postgres, redis), `.env.example` — проверить, что `make test` зелёный на заглушках
- [x] P1-02 `app/stages.py`: чтение промпта из `.claude/commands/`, вызов Anthropic, `StageResult` с файлами стадии (запись на диск — у вызывающего); тесты без сети
- [x] P1-03 `make run-text`: текст → intake → brief(batch, без диалога: «хватит» сразу) → prd → decompose; артефакты в `outputs/`
- [x] P1-04 Валидация `issues.json`: ids уникальны, `depends_on` существуют, нет циклов; тест на фикстуре
- [x] P1-05 `app/trello.py` (транспорт) и `app/publish.py` (отображение): списки по фазам, карточки, чеклист DoD, labels по area, отложенные скоупы в `Backlog`; идемпотентность по маркеру `dictum:<id>` на доске; тесты с замоканным httpx (respx)
- [x] P1-06 Первый реальный прогон на `fixtures/idea_text.md` → доска Trello; скриншот в `docs/`
  - [x] Текст → `issues.json` вживую: 10 issues, 2 фазы, граф без циклов; валидатор чист. Стоило четыре вызова Sonnet, ~55 с на первые три стадии и ~112 с на prd с decompose
  - [x] Публикация в Trello: 16 карточек, повтор без записей, досборка недоделанной карточки — `docs/p1-06-run.md`. Прогон нашёл порядок списков задом наперёд, починено там же
  - [x] Скриншот доски в `docs/trello-board.png`
- [x] P1-07 Сквозные ключи задач `PROJECT_KEY-N`: выдаются при публикации, продолжают нумерацию доски; маркер несёт ключ, `run_id` и локальный идентификатор, поэтому `I-001` разных прогонов больше не сталкиваются. Отложенные скоупы получили `title` от `/decompose` и попадают в `Backlog` с именем, а не с шифром

## Фаза 2 — Голосовое до Trello  ✓ закрыта

**Объём фазы.** Голосовое в Telegram → карточки в Trello. `auto_approve`, один
пользователь, состояние в памяти процесса, статус-сообщение после каждой стадии, ссылка на доску в
конце. Текст — тот же путь, запасной вход, если голосовое не пройдёт при людях.

**Что в фазу не вошло.** Файлов с диска и consent, ресёрча (идёт в `skip`). Ворота, Postgres и
brief-диалог приехали по ходу фазы (P2-02, P2-03, P2-04), и `auto_approve` из строчки в коде стал
флагом `.env`.

Задачи идут строго в этом порядке: каждая следующая опирается на предыдущую.

- [x] P2-00 Хребет пайплайна: делать **до** бота, потому что оба пункта дублируются в воркере, как только он появится (`docs/architecture-review.md`)
  - [x] Список стадий данными (`SPEC §7.2`): имя, исполнитель `llm`/`code`, входы, выходы, параметры. `cli` идёт по списку от стадии в `--from`; лесенка `if from_here <= N` уходит, `STAGE_ORDER` и `STAGES` схлопываются в одну запись
  - [x] `research` — запись списка с исполнителем `code`, а не заглушка, записанная в обход списка веткой в `cli`
  - [x] `run_id` от `ingest` (`SPEC §3.1`, `§5`): выдаётся при записи `transcript.md`, доезжает до `issues.json`, `publish` его только читает и падает, если поля нет. Формат маркера не меняется, доску не чистим
  - [x] Ожидание для P2-01: `ingest` и `publish` входят записями в тот же список, а не заводят у бота свой порядок. Пайплайн бота — тот же список от `ingest` до `publish`, отличается только начало и конец обхода
- [x] P2-01 Бот: режется надвое, потому что обход тестируется без Telegram и без сети, а бот — нет
  - [x] Общий обход: `ingest` и `publish` — записи того же списка стадий, `walk` в `app/run.py`, корень артефактов у прогона свой (`runs/<run_id>/` у бота, текущий каталог у CLI). Дорога у бота и у `make run-text` одна, копии порядка больше нет
  - [x] Бот polling: `/start`, текст → весь пайплайн → publish. Статус-сообщение после каждой стадии, ссылка на доску в конце. Состояние прогона живёт в памяти процесса: один прогон за раз: второй рядом с первым оплачивается вдвойне, а Postgres — это P2-02
    - [x] **Первым делом — белый список `chat_id` в `.env`.** Без него бот тратит деньги по сообщению от любого, кто нашёл его по имени, и это вопрос времени. Условие первого запуска, не отделочная задача
    - [x] Пайплайн синхронный, бот асинхронный (`CONVENTIONS`): обход уходит в `asyncio.to_thread`, иначе бот молчит три минуты
    - [x] Второй прогон при живом первом — отказ с объяснением, а не очередь. Замок оказался недостижим: PTB по умолчанию разбирает обновления по одному, и второе сообщение ждало конца первого прогона молча, а потом заводило свой. Найдено живой проверкой 08.09.2026 (три сообщения подряд → три оплаченных прогона), починено `concurrent_updates(True)` и проверено вживую в тот же день: отказ пришёл сразу, второго прогона не завелось
- [x] P2-05 `auto_approve` режимом прогона: ворот ещё нет, но флаг и его семантика в `SPEC §3.2` уже описаны, и бот идёт именно по этой ветке, а не в обход неё. Когда ворота появятся, включать будет нечего
  - [x] Ворота — поле записи стадии (`gate_after`), а не ветка у вызывающего: колонка «Ворота после» из `SPEC §3` стала данными, и копия порядка не заводится у бота с воркером
  - [x] Обход отдаёт остановку с родом: выбор идеи из нескольких `auto_approve` не снимает — подтверждать там нечего, а выбрать за человека значит выдумать данные. Ограничение режима без ворот записано в `SPEC §3.2`
  - [x] `make run-text TEXT="…" ARGS=--gates`: ветку с воротами можно пройти руками до P2-03, а не только в тестах. Рецепт `run-text` получил `ARGS`, иначе флаг было не передать
  - [x] Живая проверка на `fixtures/idea_text.md`: обход встал после brief, код `2`, сообщение назвало артефакт и `--from research`. Прогон нашёл, что логгер CLI звался `__name__`, а под `python -m app.cli` это `__main__` мимо дерева `app`: свои сообщения CLI глотал, и «Идей несколько» человек тоже никогда не видел. Починено там же
- [x] P3-01 Голосовое `.oga` → ffmpeg → Whisper → `transcript.md` → дальше тот же путь, что и у текста. Главный вход
  - [x] Расшифровка — стадия `ingest`, а не обработчик бота: `app/transcribe.py` держит границу (ffmpeg и Whisper), `Run.audio` включает ветку, дорога после `transcript.md` у голосового и текста одна
  - [x] Язык прогона называет Whisper: `Run.lang` до расшифровки хранит `DEFAULT_LANG`, после — распознанный, и `brief` получает уже его. Имя языка от Whisper (`russian`) переводится в код таблицей; незнакомое идёт как есть, потому что соврать про язык хуже
  - [x] Бот: голосовое, отказ длиннее 120 с по `duration` из сообщения (до скачивания и до денег), отказ с объяснением на файлы вместо молчания, проверка `ffmpeg` при старте рядом с белым списком
  - [x] Живой прогон 08.09.2026: голосовое 52 с на немецком → 15 карточек на `Dictum Demo`. Whisper 4.7 с, весь путь около трёх минут. Запись за собой убралась, `lang` пришёл от Whisper (`german → de`)
  - [x] Прогон нашёл гонку правок сообщения: последняя правка прогресса приходила после правки со ссылкой и затирала её, а лог оставался чистым. Финальная правка теперь ждёт все правки прогресса; тест на порядок падает на старом коде
  - [x] Голосовое на русском 08.09.2026 (`36c18be3f51087a6`): `lang: ru` при `DEFAULT_LANG=de`, бриф и 14 карточек по-русски — язык доехал от Whisper до доски. Ссылка в чате пришла последней: фикс гонки проверен живым прогоном, а не только тестом
  - [x] Отказ на голосовое длиннее 120 с: ни строки в логе, ни каталога в `runs/`, ни байта в OpenAI — проверка стоит до скачивания. Отказ по длине срабатывает раньше отказа по занятости, потому что он про само сообщение, а не про очередь
  - [x] Прогон нашёл, что ремонтный повтор стадии случается и на живых данных (intake ответил без тегов `<file>`, вторая попытка прошла): в замер времени его надо закладывать, а не считать три минуты гарантией
- [x] P2-06 Серия живых прогонов: пять прогонов подряд на `Dictum Demo` с очисткой доски между ними (`make clean-board`, артефакты — `make clean-runs`), замер времени и цены каждого, отчёт в `docs/`. Она отвечает на вопрос «сколько человек ждёт и во сколько это обходится», а не «работает ли»
  - [x] Отчёт — `docs/live-runs-2026-09-08.md`. Пять прогонов: дошли четыре, ожидание 213-287 с при медиане 241, цена расчётно $0.19 за прогон. Ответ на вопрос задачи: человек ждёт около четырёх минут, и больше половины этого времени — decompose
  - [x] Прогон с телефонного хотспота при закрытой крышке прошёл целиком: 241 с, две минуты непрерывного соединения на decompose, ноль сетевых ошибок за всю серию. P4-03 остаётся в фазе 4, ноутбук как площадка держит
  - [x] Серия нашла три вещи и все три починены в тот же вечер: intake делил одну идею надвое, decompose нарезал бэклог мельче, чем по нему работают, приветствие обещало три минуты вместо четырёх. Промпты проверены тремя прогонами, включая контрольный в обратную сторону

## Фаза 3 — то, что доехало по ходу
Задачи фазы 3 брались вперемешку с фазой 2, когда живые прогоны показывали, что без них
путь голосового неполон. Незакрытые из них стоят в «Очереди» ниже, а не здесь.
- [x] P3-04-lite Бот на кандидатах показывает список найденных идей с номерами и просит прислать нужную отдельным сообщением. Без кнопок: они в P3-07. Ветка встретилась дважды за два дня живых прогонов, а человек не видел, между чем ему выбирать
- [x] P3-06 Контракт `candidates.md`: intake заявляет `outcome: multiple | none`, форма строки объявлена в промпте и проверяется на выходе стадии. Ревью P3-04 нашло, что случай «задания нет» код узнавал по пустоте разбора, а промпт велел писать список и в нём: на записи без задания человек получал темы разговора, поданные как идеи
- [x] P3-04 Продолжение прогона после выбора: бот держит `chat_id → run_id`, ответ человека (номер или текст) уходит повтором intake с историей и `user_edit` «выбрана идея N», intake пишет `idea.md`, обход продолжается с brief в том же прогоне. Остановка бросала прогон вместе с расшифровкой, и человек диктовал идею заново по памяти. Идёт до P2-02: без него кнопки (P3-07) будут красивым способом начать сначала
  - [x] `Redo` — зеркало `Pause`: остановка выходит из обхода, правка заходит обратно в ту же стадию и только в неё. Прошлый ответ стадии для истории собирается из артефакта, а не хранится сырым: сырой текст прогон не переживёт, а файл переживёт и в P2-02
  - [x] Бот: остановка по `chat_id` в памяти процесса, цифра из списка становится «Выбрана идея N: Название», прочий текст — правкой как есть. Голосовое всегда начинает новый прогон и снимает остановку (пересмотр — P3-10), `/start` — тот же сброс, таймаута нет. При `outcome: none` ждать нечего, остановка не заводится
  - [x] Живой прогон 1d03c663aad86f72 09.09.2026: голосовое на 36 с с двумя независимыми идеями → «1» → 12 карточек за 186 с. `idea.md` только про онбординг, второй идеи в нём нет нигде; каталог, маркеры карточек (`run:1d03c663aad86f72`) и расшифровка — те же
  - [x] Остановку записывает конец прогона, а не обработчик: первый живой прогон (664534620c9a1c10) показал, что её записывал только текстовый обработчик, и «1» после голосового уходило новым прогоном. Тесты были зелёные, а главный вход — сломан
  - [x] Повтор по правке не принимает ветку, которая его вызвала: после выбора у intake остаётся один выход, `idea.md`. Второй список — претензия к форме с одним ремонтным повтором, дальше `StageError`: показать человеку тот же вопрос второй раз хуже, чем упасть
  - [x] Края остановки после ревью: список живёт в самой остановке (второе чтение файла роняло обработчик молча), номер считается только десятичным, пустое сообщение — отказ, остановка снимается лишь после удачного повтора, `/start` берёт замок прогона. Сужение выходов работает только при `choice`: на воротах отдать артефакт заново — работа стадии
  - [x] Живой прогон 96fc80909ebbf0c1 на исправленном коде: те же две идеи → «1» → 11 карточек за 187 с, `idea.md` только про онбординг, цитата дословная вместе с огрехом расшифровки
  - [x] Живой прогон 7eb77523ea80063a после прохода по ревью: 21 с записи → «1» → 11 карточек за 171 с. Расшифровка потрёпана («что погасить?»), и стадия не стала её чинить: цитата дословная, а само место вынесено в «неясное» (`CLAUDE.md` §5)
- [x] P2-02 Строка `runs` в Postgres + Alembic; состояние прогона переезжает из памяти процесса в базу. Владение фактами решено заранее в `SPEC §4.1`: строка владеет идентичностью, статусом, языком, остановкой и решениями ворот, `Run` — представление на время обхода, артефакты остаются файлами
  - [x] `app/store.py` и миграция `0001_runs`: статус — имя стадии, остановка на выборе — статус плюс место, куда возвращаться. Частичный уникальный индекс не даёт чату держать две остановки: вторая означала бы, что ответ уходит непонятно в какой прогон
  - [x] Обход о базе не знает: `walk` и `make run-text` работают без Postgres, статус пишет бот из `on_done`. Знай о ней обход — локальный прогон стадии потребовал бы поднятой базы
  - [x] Прогон, не переживший процесс, закрывается на старте, и человеку в чат уходит «бот перезапустился, прогон прерван»: строка знает чат, а до неё бот о брошенных прогонах не знал вовсе
  - [x] Тесты store против живого Postgres (`@pytest.mark.db`, пропуск без базы), включая сверку миграции с моделями через `compare_metadata`: индекс и типы объявлены дважды, и разойтись им нечем помешать
  - [x] Порт 5434 и имя `dictum` вместо `i2b`: 5432 и 5433 на машине заняты Postgres другого проекта
  - [x] После ревью: тесты работают в отдельной базе `dictum_test` (раньше чистили рабочую базу и стёрли строки живого прогона), статус пишется по законченной стадии вместе с очисткой места остановки, сорванное скачивание голосового закрывает строку, ошибка расшифровки — `failed`, а не `no_task`, схема проверяется на старте, `pool_pre_ping` держит соединение после сна ноутбука
  - [x] Живой прогон 09.09.2026: голосовое с двумя идеями → остановка → `pkill` бота → старт заново → строка `598992a2bebc7ece` на месте с `awaiting_choice` и местом возврата. Перезапуск остановку не теряет
  - [x] Там же проверены исходы строк: голосовое поверх остановки закрыло её `dropped` (бот не ломался, человек ушёл от выбора), одиночная идея дошла до `published` с 12 карточками, одинокая «1» после этого завела текстовый прогон и честно кончилась `no_task`. «1», присланная во время прогона, получила `refusal=busy` — до остановки дело не дошло
- [x] P2-03 Ворота с inline-кнопками после brief и decompose; «Править» → `user_edit` → перезапуск стадии
  - [x] Ворота — вторая остановка того же рода, что и выбор: статус `awaiting_gate`, те же `stopped_stage` и `stopped_artifact`, тот же уникальный индекс на чат (миграция `0002_gate_stop`). Отдельной колонки под род нет: выбор и ворота отличаются только тем, чего ждут от человека
  - [x] Таблицы `gates` в задаче нет, хотя `SPEC §4` её к ней обещал: писать в неё есть кому, а читать некому — место остановки живёт в строке, решения считаются по логу (`gate=next|edit|stop`). Заводится, когда историю решений кто-то спросит (ближе всего P2-04). Решение подтверждено 09.09.2026, `SPEC §4` поправлен
  - [x] «Дальше» продолжает обход со **следующей** стадии (`pipeline.after`), «Править» — с той же. Раньше `Pause.stage` уходил бы в `start` как есть, и первый же gate-повтор оплатил бы законченную стадию второй раз
  - [x] Прогон, продолженный после остановки, берёт из строки язык, режим ворот и источник: `continued()` шёл через `demo_run` с жёстким `auto_approve=True` (прогон с воротами доехал бы до доски без них), а голосовой прогон в прогрессе подписывался «принял идею»
  - [x] `AUTO_APPROVE` в `.env`, умолчание `false`: ворота — норма (`CLAUDE.md` §1), быстрый режим включается своим файлом, режим виден в логе на старте
  - [x] В сообщении ворот — числа из самого артефакта (имя и открытые вопросы брифа, задачи и фазы бэклога), сам артефакт уходит файлом: `issues.md` на дюжине задач в сообщение не влезает, а подтверждать невиданное — не ворота
  - [x] `SPEC §3.1`, `§3.3` и `CLAUDE.md` §8 помечают `consent_confirmed` и `brief_dialog` будущими колонками (P3-02 и P2-04): в `§4` их нет, а ссылки на них были
  - [x] Живой прогон `4b128388f2020797` 09.09.2026 с `AUTO_APPROVE=false`: голосовое 23 с → ворота после brief за 51 с → «Править» с двумя ограничениями → те же ворота с переделанным брифом за 21 с → «Дальше» → ворота после decompose → «Дальше» → 13 карточек. Оба ограничения доехали в бриф и легли на доску отложенными скоупами (DCT-250 «SMS-канал», DCT-251 «Интеграция с 1С»), открытых вопросов стало 13 вместо 15: правка вышла точечной, а не пересборкой
  - [x] «Стоп» на прогоне `a592c483a787fa54`: строка закрылась `dropped`, место остановки очищено, нового прогона не завелось. Кнопка от законченного прогона — `refusal=stale_button` с его номером
  - [x] Проверка нашла четыре дыры в логе, все починены в тот же вечер: `/start` не оставлял записи вовсе, старт прогона не называл ни чата, ни номера, строки стадий шли без `run_id` (`CONVENTIONS` требует его в каждой записи вызова), а `fileConfig` алембика гасил логгеры `app.*` после миграции в том же процессе. Разбор живой проверки на этом застревал: «нажал, а в логе пусто» нельзя было отличить от «не дошло»
  - [x] Тест однопрогонности доходил до настоящего Postgres и падал на блокировке, стоило поднять бота рядом: `make test` во время проверки был красным не из-за кода
  - [x] **Задача переоткрывалась после ревью** (`/rigorous critique`, две оси на чистом контексте): кнопка называла только прогон, и проверка на протухание сверяла только его, а «Править» кнопок не снимает нарочно. Живое сообщение прошлых ворот проходило проверку и уводило обход со стадии, где прогон стоит сейчас: после ворот decompose нажатое на нём «Дальше» означало `publish` — бэклог на доску без подтверждения, ровно то, против чего задача и делалась. Кнопка теперь называет и ворота; тест идёт той самой последовательностью и на старом коде падает на «опубликовал в Trello»
  - [x] Там же починено: отправка артефакта стояла перед сообщением с кнопками и без прикрытия (сбой оставлял строку в `awaiting_gate`, а человека — с галочками без выхода); `/start` был единственным обработчиком без белого списка; `SPEC §4.1` всё ещё обещал таблицу `gates` и знал только остановку на выборе; `Ending` носил статус, который при остановке дублировал её род; содержание ворот выбиралось по артефакту, а не по стадии; кнопка прошлой формы роняла обработчик молчанием
  - [x] Ревью нашло и две дыры в тестах, обещанных планом: продолженный прогон ни разу не доходил до вторых ворот, а ветка «голосовой прогон на воротах» не проверялась вовсе

## Очередь

Порядок задан и держится: следующая задача — первая незакрытая строка списка.

1. - [ ] P3-08 Пересмотр под новую цель: проект стал личным инструментом разбора записанных созвонов на немецком (`CLAUDE.md`, `SPEC §1`). Три вопроса закрыты решениями владельца 17.09.2026, работа разбита на три фазы, у каждой свой shape и craft
   - [x] Язык результата: немецкий оригинал и русский перевод рядом, анализ только на языке владельца. Если запись уже на языке владельца, перевода нет. Язык записи остаётся в `runs.lang`, язык владельца — настройка `OWNER_LANG`, её отпечаток ложится в `review.json` (`CLAUDE.md` §4, `SPEC §4.1`)
   - [x] Форма результата: сначала разбор встречи со всеми поручениями, самостоятельный; затем по кнопке одно поручение раскладывается на шаги и карточки
   - [x] Внешние сервисы: техника прежняя, звук в OpenAI, текст в Anthropic. Разрешение участников владелец спрашивает сам, вопрос бота о согласии остаётся, P3-05 в очереди не поднимается (`SPEC §8`)
   - Факты для решения. Лимит файла в 20 минут держит не Whisper: перекодировка даёт 24 кбит/с (перемерено 17.09.2026: 60 с → 180 КБ), час весит около 11 МБ при пределе Whisper 25 МБ. Поднять лимит мешает таймаут запроса 120 с в `app/transcribe.py`. Настоящее узкое место — предел Telegram в 20 МБ на скачивание (`getFile`): 20 МБ несжатого wav — это около 10 минут
   - [x] Фаза 1, разбор встречи. Новая LLM-стадия `review` (`/review`, `app/review.py`): прогон записи `ingest → review` закрывается `reviewed` или `no_task`, бот отдаёт оглавление, сообщение на каждое поручение, `review.md` и `inputs/transcript.md`. Два маршрута данными в списке стадий: остановки, заведённые до выкладки, доходят до карточек, `make run-text` без `--from` кончается разбором
     - [x] Несошедшийся дословный фрагмент после единственного ремонта не роняет разбор, а получает `in_transcript: false` и пометку в рендере; в лог идёт `unverified=<N>` (`SPEC §7`)
     - [x] Кому поручено, пишется обращением, как оно прозвучало (`du (имя не названо)`), а не `[уточнить: кому]` у каждого поручения встречи один на один. Суть не сообщает отдельным предложением, что причину не назвали
     - [x] Граница коммитов: стадия в списке и концовка бота с оглавлением и статусом ушли одним коммитом. Отдельно стадия оставила бы бота с обходом, кончающимся на `review`, и концовкой, ждущей карточек: человек получил бы «файлы прогона не нашлись». Сообщения поручений и документы — следующим коммитом
     - [x] Продолженная остановка показывает в прогрессе маршрут поручения, от `intake`: строки расшифровки у неё больше нет, расшифровка на другом маршруте
     - [ ] Живая проверка, закрывает фазу. Каждый вызов только с отдельного «ок» владельца: `make db` (база стоит на `0003_brief_dialog`, голова `0004_consent_confirmed`); немецкий текст `fixtures/transcript_meeting_de.md` без frontmatter сообщением (правило перевода здесь проверяет промпт, не код: у текста `lang` это `DEFAULT_LANG`); немецкая запись встречи 5–10 минут файлом (заодно не хоженный вживую путь P3-02); русское голосовое с одним поручением (перевода нет). По каждому: токены, был ли ремонт, `unverified=<N>` и чем был каждый помеченный фрагмент, время от «Да» до последнего документа. Качество перевода и сути, полноту поручений и честность пометок оценивает владелец
       - [x] Немецкий текст сообщением: 15 с, 4557 токенов на вход и 1585 на выход, без ремонта, `unverified=0`, оба поручения найдены, фрагменты скопированы с ошибкой распознавания. Владелец нашёл три недочёта промпта, все правятся в `/review`: «не надо» с отрицанием внутри (`do_not` «не трогать API»), пропавшая срочность («das ist nicht so dringend»), переведённое имя («Юлия» вместо «Julia»)
       - [ ] Эти три правки вживую не проверены: повторить тот же текст сообщением
   - [ ] Фаза 2, поручение в карточки. Кнопка «Разложить на шаги» под сообщением поручения, дочерний прогон (`runs.parent_id`, `runs.assignment`), `idea.md` собирает код из разбора, `brief`/`prd`/`decompose` переписаны под поручение одного разработчика, `candidates.md` и остановка `choice` уходят. До shape решить: хранение расшифровок встреч (кнопка живёт, пока на диске лежит расшифровка родителя) и язык карточек Trello
   - [ ] Фаза 3, длинные встречи: запись длиннее 20 минут. Пересобрать P3-09 и P3-03 вокруг встречи как главного входа; P3-10 делается до фазы 2 или вместе с ней
2. - [ ] P3-09 Одна граница длины для голосового и файла. После P3-08 главный вход — запись встречи, и граница решается вокруг неё, вместе с P3-03 (фаза 3 P3-08). Голосовое сегодня ограничено 2 минутами (`MAX_VOICE_MINUTES`), файл — 20 минутами, и у короткого лимита больше нет причины. Граница опирается на факты из P3-08: 20 МБ на скачивание и таймаут 120 с у расшифровки. В коде место помечено `TODO(P3-09)`
3. - [ ] P3-10 Делается до фазы 2 P3-08 или вместе с ней: кнопка поручения при живой остановке встретит тот же отказ, что и новая запись. Новая запись при живой остановке получает отказ с напоминанием про `/start` и «Стоп», а не снимает остановку: у одного человека остановка всегда его собственная и уже оплачена. Сегодня голосовое снимает её и начинает новый прогон (`SPEC §7.3`), файл снимает после замера длительности; идёт ли файл тем же отказом — решается в задаче. После неё у остановки два выхода, а не три: правится причина отказа от таймаута в `SPEC §3.2`. В коде место помечено `TODO(P3-10)`
4. - [x] `/gates on|off` для чата: умолчание режима стало состоянием чата, а не только строкой в `.env`. Ворота включаются, не гася бота
   - [x] Команда меняет умолчание чата, а не идущий прогон: его режим записан в строке (`SPEC §4.1`), и менять его на ходу значило бы соврать про прогон, который человек оплатил
   - [x] Состояние — словарь в памяти процесса, без колонки и миграции: перезапуск возвращает умолчание к флагу `.env`, который бот печатает на старте, и терять там нечего. Колонку заводит P4-03, когда webhook и исполнитель разъедутся по процессам и словарь перестанет быть правдой для второго
   - [x] Живая проверка вместе с воротами, прогон `fc82346678810a49` 09.09.2026: `/gates on` (бот поднят с `AUTO_APPROVE=true`, строка прогона получила `auto_approve=f`) → голосовое 23 с → ворота brief за 48 с → «Править» + текст → те же ворота с новым брифом за 21 с → «Дальше» (обход пошёл с `research`, brief не переигран) → ворота decompose → «Дальше» → 11 карточек. Доска: 246 до прогона, 257 после — ровно `finished cards=11`
   - [x] Там же протухшая кнопка: «Дальше» на первом сообщении ворот brief дало `refusal=stale_button … gate=brief`, и доска осталась на 257. Ровно та ветка, которую нашло ревью
   - [x] Первая попытка проверки (`2ebcf8868ca3dbf9`) умерла на decompose: модель прислала вместе с `issues.json` ещё и `issues.md` вопреки промпту, и прогон встал на проверке набора, потратив 9585 токенов выхода. Починено в тот же час — лишний файл при полном наборе выбрасывается со строкой `extra=` в логе (`SPEC §7`), а правило единственного файла уехало в конец `/decompose` последним блоком
   - [x] Ещё раньше проверка не состоялась вовсе: `/gates on` до бота не дошло, прогон `8711ba0dabc9fb8e` завёлся с `auto_approve=t` и опубликовал 10 карточек без ворот. Строка в базе и сплошной обход без остановок показали это раньше, чем память участников
5. - [x] P3-07 Выбор идеи кнопкой вместо ответа сообщением: поверх продолжения прогона (P3-04) и кнопок ворот (P2-03). Последняя часть чата, которая всё ещё требует набирать текст
   - [x] Кнопка и набранный номер — один ответ: обе ветки идут через `choice_answer`, и разойтись двум входам в один уговор нечем. Ответить словами по-прежнему можно, кнопка заменяет набор номера, а не текстовый ответ
   - [x] В строке ответа появился `by=button|text`: кнопку заводили ради того, чтобы не набирать номер, и ответ на «пользовались ли ею» берётся счётом по логу, а не памятью участников
   - [x] Кнопка называет прогон, но не место остановки, в отличие от воротной: выбор у прогона один — повтор по нему сужает выход intake до `idea.md` (`SPEC §7`). Зато сверяется род остановки: нажатие на живом списке, когда прогон стоит уже на воротах, вернуло бы обход в intake и выбросило бриф, которого человек ждал
   - [x] Клавиатура после нажатия не снимается, как и на «Править»: сорвавшийся повтор возвращает остановку и просит ответить ещё раз, а снятая отняла бы единственный способ это сделать
   - [x] Клавиатуру собирает тот, кто читал `candidates.md`, и она едет в `Ending` рядом с текстом: второе чтение того же файла ради списка однажды роняло обработчик молча (P3-04)
   - [x] Живой прогон `18c0b3569bad7564` 09.09.2026 (`AUTO_APPROVE=true`, ворота сняты нарочно — проверялся выбор, а не они): голосовое 20 с с двумя идеями → список за 9 с → кнопка «1» → 12 карточек за 168 с. В логе `answer=choice by=button`, повтор intake отдал только `inputs/idea.md`, и второй идеи (утренняя сводка) в нём нет нигде
   - [x] «2» на том же списке после финала: `refusal=stale_button` с номером прогона, и за весь лог ровно 12 строк «Создана карточка» — доска не шевельнулась. Это ветка «прогон уже не ждёт»; ветку «прогон ждёт, но ворот, а не выбора» держит тест, вживую её не проходили
6. - [x] **Серия живых прогонов**: пять прогонов подряд по сценарию (голосовое → ворота → правка → карточки, плюс выбор идеи кнопкой), замер времени и цены, отчёт в `docs/`
   - [x] Отчёт — `docs/live-runs-2026-09-09.md`, код `900ad89`. Пять из пяти дошли до доски, ожидание 198–241 с при медиане 216, расчётно $0.21 за прогон. P2-06 давал 4 из 5 и медиану 241: худший случай стал лучше прежней медианы, причём два прогона несли остановки с человеком внутри
   - [x] Доска между прогонами не чистилась нарочно: карточки пяти прогонов лежали рядом, и маркеры развели их сами. За 22 вызова модели ноль warning, ноль error и ноль ремонтных повторов — вилка времени получена без них, и повтор её удлинит
   - [x] Серия нашла три вещи, все три записаны в отчёт: каркас `issues.md` не переводится на язык прогона (английские «Backlog» и «DoD» в немецком документе — расхождение с `CLAUDE.md` §4), `refusal=busy` на кнопке не называет решение, `/intake` перевёл «Суть» как «## Suche»
   - [x] Первая и третья оказались одним решением, и оно противоположно тому, как находка была записана: правило поменяли, а не код. Заголовки и подписи всех артефактов фиксированы на английском при любом `lang`, содержимое — на языке прогона (`CLAUDE.md` §4, `SPEC §5`); `intake`, `brief`, шаблон PRD и `decompose` приведены к нему. Так уже писал `issues.md` код, и переводить заголовки значило бы заводить словарь на каждый язык
   - [x] Вторая: `refusal=busy` от кнопки называет `gate=<стадия> press=<решение>`, у выбора — `pick=<номер>`
   - [ ] Всё это без живых прогонов: правки сделаны после серии 09.09.2026, проверка — живым немецким прогоном, иначе правило проверено не будет
   - [x] Расхождение цены из P2-06 закрыто: там расчёт и консоль разошлись в двадцать раз, сегодня обе цифры одного порядка ($1.06 расчётно за серию против $0.93 по консоли за весь день). Гипотеза «ключ считается не в том воркспейсе» снимается
7. - [x] P2-04 brief-диалог: вопрос → ответ → следующий вопрос; «Собирай». Здесь же заводится `runs.brief_dialog` и, если история решений понадобится ей, таблица `gates` (`SPEC §4`)
   - [x] Вопрос — второй выход стадии, а не текст мимо контракта: `outputs/brief_question.md` объявлен веткой `outputs` рядом с `brief.md`, ровно как `candidates.md` у intake. Ход обратно в стадию уже был, и заводить второй канал ради разговора было нечем: режим API запрещает всё вне тегов `<file>`, и вопрос прозой до человека бы не дошёл
   - [x] `interactive` перестал быть константой в записи стадии: есть ли кому отвечать, знает вход. Бот включает диалог там же, где ворота, — человек, готовый подтверждать, готов и отвечать, а одному человеку хватает одного переключателя. Локальный прогон держит `false` даже с `--gates`: ответить ему нечем (`CLAUDE.md` §1)
   - [x] Ходы копит `runs.brief_dialog` (миграция `0003_brief_dialog`): ответы человека не лежат ни в одном артефакте, а стадии нужны все сразу. Текущий вопрос в колонку не идёт — он лежит файлом и замыкает историю тем же `previous_answer`, что и любой другой повтор
   - [x] Таблицу `gates` задача не завела, хотя `SPEC §4` считал её ближайшим поводом: диалог хранит свои ходы сам, а историю решений на воротах по-прежнему некому читать. Условие в `SPEC §4` переписано, а не снято
   - [x] Разговор кончают кнопка «Собирай» и бюджет `MAX_BRIEF_QUESTIONS` (умолчание 5) — одинаково: стадии это говорится словами и подпирается запретом ветки. Одних слов мало (лишний вопрос вернул бы человека туда же), одного запрета мало (о конце разговора стадия узнала бы отказом после оплаченного вызова). Запрет уехал из рода остановки в поле `Redo.closes_branch`: один и тот же ответ то кончает диалог, то нет
   - [x] «Собирай» хода не пишет: человек не ответил на вопрос, а прекратил разговор, и ответ за него был бы выдуманными данными (`CLAUDE.md` §5)
   - [x] Живая проверка стадии, прогон `d445293535732fba` 09.09.2026 (три вызова, без Telegram и Trello, `MAX_BRIEF_QUESTIONS=2`): brief отдал `brief_question.md`, а не бриф; оба вопроса — одно предложение в 85 и 87 знаков, проверка формы чиста; ответ вернулся в стадию с историей; на исчерпанном бюджете стадия написала бриф и третьего вопроса не задала. Форма вопроса живёт в промпте и тестами не проверяется вовсе — потому и проверялась живьём
   - [ ] Живой прогон целиком: голосовое → вопросы → «Собирай» кнопкой → ворота брифа → карточки. Кнопка, остановка в базе и продолжение после перезапуска вживую не ходили
   - [x] Та же проверка показала цену разговора: два вопроса и бриф — 11 105 токенов входа и 5 087 выхода за 66 с модельного времени, поверх остальных стадий. Три вопроса — минуты полторы сверх медианы в 216 с серии 09.09.2026
   - [x] Попутно нашлась старая поломка: `run_id` из одних цифр YAML читал числом, `run_id_of` не находил строки, и `--from` отказывался продолжать прогон, у которого id есть. Раз в тысячу восемьсот идентификаторов, то есть примерно раз в сорок полных прогонов `make test`. Починено кавычками, отдельным коммитом
8. - [x] P3-02 Файл mp3/m4a/wav: consent-кнопка, отказ без подтверждения. Здесь заводится `runs.consent_confirmed` (`CLAUDE.md` §8)
   - [x] Согласие спрашивается до скачивания, а не после: до «Да, все согласны» нет ни файла на диске, ни строки в `runs`, ни потраченного цента. Скачать сначала значило бы держать у себя чужую запись без согласия
   - [x] Строки со статусом `awaiting_consent` нет: у согласия ещё нет прогона, а четвёртый род остановки ломал бы частичный индекс `ux_runs_one_stop_per_chat`. Запись кнопка не несёт (`file_id` не влезает в 64 байта `callback_data`): её берут из сообщения, на которое отвечает вопрос. Поэтому вопрос уходит с `do_quote=True`: в личном чате PTB без него не цитирует, и у кнопки не нашлось бы файла
   - [x] Принимаются и `Audio`, и документ `audio/*`: wav приходит документом. Отказ до вопроса по 20 МБ (предел `getFile`) у обоих и по 20 минутам у аудио; после скачивания длину меряет `ffprobe` у любого файла, потому что длительность аудио называет клиент и бывает неверной
   - [x] Длинный файл закрывается новым статусом `refused`, а не `no_task`: тот говорит о содержимом, а эту запись никто не слушал. Файл удаляется при любом `KEEP_AUDIO`, остановка в чате снимается только после замера: отвергнутый файл не должен стоить человеку его ответа на воротах
   - [x] В логе `consent=yes|no` с `by=` нажавшего, а не приславшего: в группе это разные люди. `ConsentMissing` своей ветки в боте не получил: путь недостижим по построению, и общий «прогон сорвался» с трассировкой там честный ответ
   - [x] `ffprobe` проверяется на старте рядом с `ffmpeg`
   - [x] Правки по ревью: `busy` на кнопке согласия пишет `press=yes`, а не `consent=yes`, и журнал согласий по `grep` больше не засчитывает отвергнутое нажатие; поздний `file_too_long` несёт `run=`. Запись удаляется при любом исходе расшифровки, сбой `ffprobe` удаляет файл, как длинный, и не показывает человеку путь на сервере. Клавиатура не снимается молча: ответ и время дописываются в сам вопрос, сорванная правка пишет `consent_question=kept`. `Run.source` стал обязательным: с умолчанием `text` забытый источник проходил мимо `ConsentMissing`. Двойник базы в тестах бота держит правило `ck_runs_consent_only_for_file`. Тексты называют форматы примерами: фильтр пропускает любой `audio/*`
   - [ ] Живая проверка: mp3 как аудио, wav как документ, «Нет» (ни файла в `runs/`, ни строки), файл длиннее 20 минут (`refused`, файла нет), нажатие «Да» в групповом чате не тем, кто прислал (в логе `by=` нажавшего)
9. - [ ] P3-03 Нарезка длинного аудио по тишине, склейка. Пересобирается вместе с P3-09 вокруг встречи как главного входа (фаза 3 P3-08)
10. - [ ] P4-01 `/research` market-режим с веб-поиском; `kind: feature` → skip
11. - [ ] P4-02 Логи токенов и стоимости на run
12. - [ ] P4-03 Деплой на Hetzner, webhook вместо polling
    - [ ] Деплой меняет площадку: задача закрывается живым прогоном целиком, а не по диффу
13. - [ ] P3-05 self-hosted Whisper на Hetzner: сейчас расшифровка уходит в OpenAI, то есть за пределы EU (`SPEC §8`)
    - [ ] Свой Whisper меняет расшифровку: задача закрывается живым прогоном целиком, а не по диффу

## Не в очереди
Ждут решения или повода, порядка между ними нет.

- [ ] Ответ модели мимо тегов не считается никем. Живая проверка P2-04 показала 912 и 2408 токенов выхода на вопросы в 85 и 87 знаков: остальное — рассуждение вне `<file>`, которое `parse_file_blocks` молча выбрасывает. Платим и ждём мы за него наравне с делом. Лишний **файл** при полном наборе считается строкой `extra=` (`SPEC §7`), а текст вне тегов — нет, и дрейф промпта по нему не измерить. Решать надо два вопроса разом: считать ли (строка в логе) и давить ли (строка в промпте)

- [x] Сорванная расшифровка оставляла запись на диске и при `KEEP_AUDIO=false`: удаление в `transcribe` стояло после успеха. Для своего голосового это было терпимо, для чужой записи с диктофона уже нет. Теперь при `KEEP_AUDIO=false` запись и её перекодировка удаляются при любом исходе: сбой ffmpeg, сбой Whisper, пустая расшифровка. Сбой `ffprobe` в боте удаляет файл при любом `KEEP_AUDIO`, как и длинный. Сорванное скачивание файла не оставляет: PTB пишет его на диск одним куском после того, как получил целиком
- [ ] Принятый риск: `file_size` в сообщении может не прийти, и тогда файл больше 20 МБ проходит проверку до вопроса, а после «Да» получает «не смог забрать запись» (`FILE_NOT_TAKEN`, строка `failed`) вместо честного `too_big`
- [ ] Принятый риск: согласие даёт любой участник разрешённого чата. Проверки, что нажавший есть на записи, нет: в логе остаётся его `by=`, и только
- [x] Таймаут ворот и выбора 24 ч → `stalled` не делается: у одного человека три выхода из остановки (голосовое, `/start`, «Стоп»), а таймаут выбросил бы оплаченный прогон. `SPEC §3.2` и `§4` больше не обещают `stalled`
- [ ] Условные ворота после intake по `confidence: low` (`SPEC §3.2`): признак ворот в списке стадий сегодня безусловный (`gate_after`), а у `idea.md` нет своего краткого содержания
- [ ] Текст сбоя `convert_to_mp3` показывает человеку stderr ffmpeg с полным путём на сервере — та же поломка, что уже починена у `ffprobe` (P3-02)
- [ ] `SPEC §8` обещает «≤ 3 параллельных run на chat_id», а бот держит ровно один прогон за раз с отказом `busy` (`SPEC §7.3`). Одно из двух мест врёт
- [x] `/decompose` не выбирал `area: design`: метка была объявлена и в `app/models.py`, и в промпте, а признака, по которому её выбирают, не было. Правило названо — экран, диалог, уведомление, пустое состояние, ошибка получают задачу дизайна с DoD «фрейм в Figma, ссылка в карточке», задачи кода по этому месту ставят её в `depends_on`. Два прогона `--from decompose` на PRD `fc82346678810a49` 09.09.2026: было `design: 0`, стало `design: 2` со ссылками. Уведомление в приложении модель экраном не считала — отсюда перечисление в правиле, и проверять его на серии живых прогонов, а не третьим прогоном
- [x] Там же нашлось неверное обещание промпта: «идентификаторы стабильны при повторном прогоне». Тот же PRD дал 8 задач и 9 с разными скоупами под теми же номерами. Обещание убрано: сквозной ключ выдаёт публикация, локальные `I-00N` живут один прогон, а внутри прогона их держит на месте прошлый ответ в истории повтора (`SPEC §7`)

## Как начинать сессию в Claude Code
Первое сообщение: «Прочитай CLAUDE.md, SPEC.md и PLAN.md. Текущая задача — первая незакрытая строка Очереди. Предложи план на 5-7 шагов, не пиши код до моего ок.»
